<h1>Edit Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p> Guidelines for Wiki Posts</p>
    <ul>
      <li>Wiki must be a unique topic, check to see if there is a topic already</li>
      <li>Wiki must be readable and exclude vulgar or vague language</li>
      <li>Spamming will not be tolereated</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= render partial: "form", locals: {wiki: @wiki } %>
  </div>
</div>
<div class="row">
  <div class="col-md-4">
  </div>
  <%  if current_user.id == @wiki.user_id || current_user.admin? %>
  <div class="col-md-8">
  <%= link_to "Manage Collaborators", wiki_collaborators_path(@wiki), class: "btn btn-info" %>
  </div>
  <% end %>
</div>
